import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.razum.fitnes',
  appName: 'Fitnes App',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
